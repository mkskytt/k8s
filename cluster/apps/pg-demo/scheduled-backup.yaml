apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-demo-daily
  namespace: pg-demo
spec:
  schedule: "0 0 2 * * *"  # 02:00 UTC every day (CNPG uses 6-field cron with leading seconds)
  backupOwnerReference: self
  cluster:
    name: pg-demo
