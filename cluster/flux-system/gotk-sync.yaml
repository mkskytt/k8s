# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  url: https://github.com/mkskytt/k8s
  # Verify the OpenPGP signature of the HEAD commit before reconciling.
  # PREREQUISITES — do NOT merge this block until both are true, or source
  # reconciliation will halt (HEAD will fail verification):
  #   1. The flux-system/flux-git-public-keys secret exists, holding the
  #      armored public key(s) of every identity allowed to sign main:
  #        gpg --export --armor <KEYID> > key.asc
  #        kubectl create secret generic flux-git-public-keys \
  #          -n flux-system --from-file=key.asc
  #   2. All commits landing on main are signed by one of those keys
  #      (enable branch protection requiring signed commits on main).
  verify:
    mode: HEAD
    secretRef:
      name: flux-git-public-keys
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./cluster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
